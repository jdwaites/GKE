apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hello-gke.fullname" . }}
  labels:
    app: {{ include "hello-gke.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "hello-gke.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "hello-gke.name" . }}
    spec:
      containers:
        - name: {{ include "hello-gke.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: {{ .Values.containerPort }}
